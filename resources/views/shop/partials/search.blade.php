<form action="{{ route('catalogo.search') }}" id="buscar">
    <div class="header-search">
        <div class="search">
            <i class="material-icons" id="lupa">search</i>
            <input type="search"
                name="query"
                placeholder="Buscar">
        </div>
    </div>
</form>

